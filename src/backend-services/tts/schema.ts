import {JSONSchemaType} from "ajv";

export type TTS_State = {

};

const Schema_TTS: JSONSchemaType<TTS_State> = {
  type: "object",
  properties: {},
  required: [],
  default: {}
}

export default Schema_TTS
